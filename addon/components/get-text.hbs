{{! template-lint-disable no-triple-curlies }}
{{#each this.messageParts as |part|~}}
  {{~#if part.name}}
    {{~yield part.text part.name}}
  {{~else~}}
    {{~#if @unescapeText~}}
      {{{part.text}}}
    {{~else~}}
      {{~part.text~}}
    {{~/if~}}
  {{~/if~}}
{{~/each}}