{{~#each messageParts as |part|}}
  {{~#if part.name~}}
    {{~yield part.text part.name}}
  {{~else~}}
    {{~#if escapeText~}}
      {{{part.text}}}
    {{~else~}}
      {{~part.text}}
    {{~/if~}}
  {{~/if~}}
{{~/each~}}
